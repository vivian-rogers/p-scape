.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
# All of data/ is local — runs, caches, OSM extracts, Valhalla tiles.
# Reproducibility lives in scripts that re-fetch / re-run; sample figures
# worth showing in the repo go under docs/figures/.
data/*
!data/.gitkeep
*.tif
.DS_Store
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/

.claude/

# Vercel: web/ is populated at deploy time by vercel.json's buildCommand
# (copies pnorm/data/explorer.html → web/index.html). Don't commit the copy.
web/
.vercel/
.vercel
